@keyframes rotate
  from
    transform: rotate(0deg)
  to
    transform: rotate(359deg)

.loader
  width 16px
  height @width
  border-left-color transparent !important
  border-bottom-color transparent !important
  border-radius 500px
  animation: rotate unquote($LOADER.speed) infinite linear
